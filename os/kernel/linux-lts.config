# Linux LTS kernel config baseline for Omos OS
# NOTE: This is a minimal placeholder. Replace with a merged config generated from:
#   make defconfig
#   scripts/kconfig/merge_config.sh -m defconfig ./os/kernel/omos-base.config
#   make olddefconfig
#
# Key requirements to ensure during merge:
# - EFI + Secure Boot support
# - ext4 enabled
# - btrfs enabled as module
# - namespaces, cgroups, seccomp
# - AppArmor enabled
# - audit enabled
# - netfilter enabled
# - overlayfs for live ISO

CONFIG_EFI=y
CONFIG_EFI_STUB=y
CONFIG_EFI_MIXED=y
CONFIG_EFIVAR_FS=y
CONFIG_EFI_SECURE_BOOT_SIG_ENFORCE=y

CONFIG_EXT4_FS=y
CONFIG_BTRFS_FS=m

CONFIG_NAMESPACES=y
CONFIG_CGROUPS=y
CONFIG_SECCOMP=y
CONFIG_SECURITY=y
CONFIG_SECURITY_APPARMOR=y
CONFIG_AUDIT=y

CONFIG_NETFILTER=y
CONFIG_NETFILTER_ADVANCED=y

CONFIG_OVERLAY_FS=y
CONFIG_SQUASHFS=y
CONFIG_SQUASHFS_XZ=y
